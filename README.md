# Полёт вслепую — навигация по рельефу без GPS

Определение координат, скорости и курса воздушного судна **без спутниковой навигации** (GPS/ГЛОНАСС) —
по форме земли под крылом. Если спутниковый сигнал заглушили или потеряли, самолёт всё равно должен
понимать, где он. Решение читает «рельеф под собой» и находит это место на заранее загруженной карте.

Метод называется **TERCOM** (Terrain Contour Matching — сопоставление профиля рельефа).

---

## Идея простыми словами

У самолёта есть два прибора:

- **радиовысотомер** — измеряет расстояние до земли прямо под собой (сколько метров «вниз»);
- **барометр** — измеряет абсолютную высоту полёта над уровнем моря.

Вычитаем одно из другого и получаем **высоту самого рельефа** в каждой точке маршрута:

```
высота земли над морем = высота полёта (барометр) − расстояние до земли (радиовысотомер)
```

Пролетая, мы собираем **профиль высот** — что-то вроде «кардиограммы» местности: холм, низина, склон.
Этот профиль уникален, как отпечаток. Дальше мы берём **карту высот** того района (DEM — Digital
Elevation Model, обычная матрица «высота в каждой точке») и ищем на ней такую линию, чей рельеф
совпадает с нашим отпечатком. Где совпало — там мы и летим.

Сравнение «похоже / не похоже» — это **корреляция**: математическая мера того, насколько две линии
повторяют форму друг друга (1.0 — идеально совпали, 0 — ничего общего).

Сложность в том, что заранее **неизвестны ни скорость, ни направление** полёта. Поэтому алгоритм
сначала перебирает множество вариантов «куда и как быстро мы могли лететь» от последней известной
точки, отбирает самые правдоподобные, а затем **ведёт** объект дальше, уточняя позицию на каждом шаге.

---

## Что реально готово: движок TERCOM (папка `егор чекни этот алгоритм/`)

Это законченное, рабочее ядро проекта. Один самодостаточный скрипт
[`tercom_final.py`](егор%20чекни%20этот%20алгоритм/tercom_final.py), которому нужны только карта высот
и записанный профиль радиовысотомера.

Как он работает по шагам:

1. **Подготовка сигнала.** Сырые показания приборов шумят (вибрация двигателя, болтанка). Их чистят
   фильтрами: медианный (убирает резкие выбросы) + сглаживающий фильтр низких частот, а барометр
   дополнительно стабилизирует фильтр Калмана (предсказывает следующее значение и подмешивает к нему
   измерение, гася шум).
2. **Первичный захват («бутстрап»).** От последней точки GPS перебираются сотни гипотез «направление ×
   скорость» и для каждой строится предполагаемый профиль рельефа с карты. Совпавшие по корреляции —
   кандидаты. Чтобы не схватить случайно похожее «чужое» место, кандидатов перепроверяют на нескольких
   длинах участка (ложное совпадение обычно разваливается на длинном окне).
3. **Сопровождение.** После захвата объект ведут дальше **фильтром Калмана** (математический «следящий
   глаз»: держит позицию и скорость, плавно поправляя их по новым измерениям). Чтобы один ошибочный
   замер не сбил трек, ведётся **несколько гипотез одновременно** (multi-hypothesis) — выживает самая
   достоверная.
4. **Финальная доводка.** Уже после прохода трек дополнительно «защёлкивают» на лучшие совпадения
   рельефа и сглаживают. Эти поправки **не вмешиваются обратно** в основной алгоритм (чтобы не
   раскачать его) и ограничены порогами, чтобы не «дорисовывать» данные там, где рельеф неоднозначен.

На выходе — папка с результатами: траектория в пикселях и в реальных координатах (широта/долгота,
скорость в м/с, курс в градусах), графики и HTML-отчёт.

![Восстановленная траектория на карте высот](егор%20чекни%20этот%20алгоритм/result/trajectory_on_dem.png)

Как выглядит сам перебор гипотез на этапе захвата — ниже. По горизонтали направление полёта (азимут,
0–360°), по вертикали скорость, цвет — насколько хорошо совпал рельеф (светлее = лучше). Алгоритм
находит чёткий максимум (звёздочка, корреляция 0.996) — это и есть верный ответ «куда и как быстро мы
летим». Тёмная зона — направления, где рельеф под нами с картой не совпадает.

![Тепловая карта совпадения по направлению и скорости](егор%20чекни%20этот%20алгоритм/result/correlation_heatmap_az_speed.png)

### Точность

На тестовом маршруте над сибирской тайгой (карта ~30 м на пиксель):

| Показатель | Результат |
|---|---|
| Финальная ошибка позиции | **≈ 10 м** (меньше половины пикселя карты) |
| Медианная ошибка по треку | **≈ 16 м** |

Это близко к **физическому пределу** для таких данных (~11 м): точнее не позволяют ни разрешение
карты, ни шум радиовысотомера, ни принципиально «похожие на чужие» участки рельефа над безориентирной
местностью. Подробный разбор — в [`ОТЧЁТ_точность.md`](егор%20чекни%20этот%20алгоритм/ОТЧЁТ_точность.md).

Ниже — ошибка положения на протяжении всего полёта (в пикселях карты). Весь маршрут она держится
**меньше одного-двух пикселей**, то есть точность стабильна, а не «повезло в одной точке».

![Ошибка позиции по времени](егор%20чекни%20этот%20алгоритм/result/error_vs_time.png)

**Честность результата:** истинная траектория алгоритму **не показывается** — она нужна только чтобы
посчитать ошибку на графиках. Старт берётся из последнего GPS-сигнала (он легитимно есть до пропадания),
курс — из «компаса». Если задать заведомо неверный курс, результат почти не меняется — значит система
действительно восстанавливает путь по рельефу, а не «подгоняет под ответ».

### Как запустить

```bash
pip install numpy scipy matplotlib rasterio tifffile

cd "егор чекни этот алгоритм"
python tercom_final.py --dem dem_package_sibir.txt --heights heights_m.txt --input-mode radio \
  --start-x 422 --start-y 297 --initial-heading-deg 95 --truth tchk.csv --refit --out-dir result
```

Обязателен только `numpy`; остальные библиотеки опциональны (без них просто отключаются графики или
часть форматов карт). Результаты появятся в папке `result/`.

### Готовые команды на типовые задачи

```bash
# 1) Честный режим «без GPS»: старт = последняя точка GPS, курс = компас.
#    truth подаётся ТОЛЬКО для подсчёта ошибки на графиках, в расчёт не идёт.
python tercom_final.py --dem dem_package_sibir.txt --heights heights_m.txt \
  --start-x 422 --start-y 297 --initial-heading-deg 95 --truth tchk.csv --out-dir result

# 2) Максимальная точность (включить доводку по корреляции — медленнее на ~1 мин)
python tercom_final.py --dem dem_package_sibir.txt --heights heights_m.txt \
  --start-x 422 --start-y 297 --initial-heading-deg 95 --refit --out-dir result

# 3) Быстрый прогон (отключить тепловую карту и постобработку)
python tercom_final.py --dem dem_package_sibir.txt --heights heights_m.txt \
  --start-x 422 --start-y 297 --initial-heading-deg 95 \
  --no-plot-heatmap --no-smooth --no-final-fix --out-dir result

# 4) Демо-режим с точным перебором направления (шаг 1°, нагляднее, но медленнее)
python tercom_final.py --dem dem_package_sibir.txt --heights heights_m.txt \
  --start-x 422 --start-y 297 --initial-heading-deg 95 \
  --bootstrap-az-step-deg 1 --heatmap-az-step-deg 1 --out-dir result

# 5) Старт в географических координатах вместо пикселей
python tercom_final.py --dem dem_package_sibir.txt --heights heights_m.txt \
  --start-lon 89.123 --start-lat 61.456 --initial-heading-deg 95 --out-dir result

# 6) Замер скорости работы (тайминги этапов в result/profile_summary.json)
python tercom_final.py --dem dem_package_sibir.txt --heights heights_m.txt \
  --start-x 422 --start-y 297 --initial-heading-deg 95 --profile --out-dir result
```

### Справочник флагов

> Значения по умолчанию подобраны под вложенные тестовые данные — для обычного прогона менять почти
> ничего не нужно. Полный список и логика — в функции `build_parser()` в конце скрипта.

<details>
<summary><b>Вход и выход</b> — какие файлы читать и куда писать</summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--dem` / `--map` | — | Карта высот: GeoTIFF, `.npy` или текстовый NPZ-пакет. **Обязателен.** |
| `--heights` | — | Профиль радиовысотомера: текстовый файл, по одной высоте (м) на строку. Основной вход. |
| `--input-mode` | `radio` | `radio` — в файле высоты радиовысотомера (фильтруются внутри); `terrain` — там уже готовая высота рельефа. |
| `--nmea` | — | Устаревшая альтернатива `--heights`: сырой NMEA-лог. Если задан и `--heights`, побеждает `--heights`. |
| `--nmea-start-index` | `0` | С какого отсчёта начать (отрезать «разгон» в начале лога). |
| `--out-dir` | `result_final` | Папка для результатов (полностью пересоздаётся). |

</details>

<details>
<summary><b>Старт и курс</b> — откуда и куда летим в начале</summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--start-x`, `--start-y` | — | Стартовая точка в пикселях карты (`x` = столбец, `y` = строка). |
| `--start-lon`, `--start-lat` | — | То же, но в географических координатах (долгота/широта). |
| `--initial-heading-deg` | — | Начальный курс: 0° = север, по часовой стрелке. Аналог «компаса». |
| `--truth` | — | Эталонная траектория (CSV). **Только для отладки** — считает ошибку и рисует графики, в навигацию не входит. Если старт не задан явно — берётся первая точка отсюда. |
| `--heading-samples` | `30` | Сколько точек эталона взять, чтобы прикинуть начальный курс (если он не задан вручную). |

</details>

<details>
<summary><b>Сенсоры и фильтрация</b> — частота, высота полёта, чистка шума</summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--fs` | `10.0` | Частота отсчётов сенсоров, Гц. |
| `--absolute-altitude` | `1500.0` | Высота полёта над морем (м) — нужна, чтобы из радиовысоты получить высоту рельефа. |
| `--radar-median` | `3` | Окно медианного фильтра радиоканала (убирает резкие выбросы). |
| `--radar-cutoff` | `3.0` | Частота среза сглаживающего фильтра радиоканала, Гц. |
| `--baro-median` | `3` | Окно медианного фильтра барометра. |
| `--baro-q` | `0.02` | «Доверие к движению» в фильтре Калмана барометра (шум процесса). |
| `--baro-r` | `1.0` | «Доверие к измерению» барометра (шум измерения). Больше — сильнее сглаживает. |
| `--baro-noise-std` | `0.0` | Добавить синтетический шум барометра (для стресс-тестов). |
| `--baro-drift-per-s` | `0.0` | Добавить медленный дрейф барометра (для стресс-тестов). |
| `--noise-seed` | — | Зерно генератора шума — для воспроизводимости. |

</details>

<details>
<summary><b>Скорость</b> — в каком диапазоне ищем</summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--min-speed-m-s` | `5.0` | Минимальная ожидаемая скорость, м/с. |
| `--max-speed-m-s` | `45.0` | Максимальная ожидаемая скорость, м/с. |
| `--min-speed-px-s`, `--max-speed-px-s` | авто | То же в пикселях/с. Если не задано — считается автоматически из масштаба карты. |

</details>

<details>
<summary><b>Захват и сопоставление (TERCOM)</b> — сердце алгоритма</summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--window-size` | `200` | Длина сравниваемого участка профиля (в отсчётах). Длиннее — надёжнее, но дороже. |
| `--max-history` | `300` | Сколько прошлых точек траектории хранить для сопоставления. |
| `--bootstrap-period` | `200` | Как часто (раз в N отсчётов) пробовать первичный захват, пока цель не поймана. |
| `--local-period` | `50` | Как часто уточнять позицию после захвата. |
| `--min-roughness-bootstrap` | `4.0` | Не пытаться захватываться на «слишком ровном» рельефе (там нельзя определиться). |
| `--min-roughness-local` | `1.0` | То же для уже идущего сопровождения. |
| `--bootstrap-az-step-deg` | `5.0` | Шаг перебора направления при захвате (меньше = точнее, но медленнее). |
| `--bootstrap-speed-step-px-s` | `0.2` | Шаг перебора скорости при захвате. |
| `--bootstrap-radius-px` | `8.0` | Радиус поиска малых сдвигов вокруг кандидата при захвате. |
| `--bootstrap-grid-px` | `4.0` | Шаг сетки этих сдвигов. |
| `--initial-azimuth-deg` | — | Подсказать центр направления при захвате (синоним `--initial-heading-deg`). |
| `--initial-az-window-deg` | `180.0` | Насколько вокруг подсказанного курса искать (±N°). 180 = весь круг. |
| `--bootstrap-verify-windows` | — | Доп. длины окон для перепроверки кандидатов (защита от ложного совпадения). |
| `--local-match-mode` | `history` | `history` — сдвиг/поворот всей истории (для ровного полёта); `segment` — короткий отрезок (для манёвров). |
| `--local-window-size` | `50` | Длина участка для сопровождения в режиме `segment`. |
| `--local-radius-px` | `10.0` | Радиус локального поиска при сопровождении. |
| `--local-grid-px` | `10.0` | Шаг сетки локального поиска. |
| `--local-az-step-deg` | `5.0` | Шаг направления при сопровождении (режим `segment`). |
| `--local-speed-step-px-s` | `0.2` | Шаг скорости при сопровождении. |
| `--local-theta-deg` | `8.0` | На сколько градусов поворачивать историю при сопоставлении (режим `history`). |
| `--local-theta-step-deg` | `4.0` | Шаг этого поворота. |
| `--local-endpoint-gate-px` | `35.0` | Не принимать кандидата дальше этого расстояния от предсказания Калмана (отсев далёких ложных). |
| `--min-corr-bootstrap` | `0.65` | Порог корреляции для захвата (ниже — не кандидат). |
| `--min-corr-local` | `0.55` | Порог корреляции при сопровождении. |
| `--min-quality-corr` | `0.55` | Порог «качественного» совпадения для оценки уверенности и доводки. |
| `--top-candidates` | `6` | Сколько лучших кандидатов оставлять на каждом шаге. |
| `--max-hypotheses` | `8` | Сколько параллельных гипотез вести (мультигипотезность). |
| `--nms-radius-px` | `8.0` | Дедупликация кандидатов: насколько близкие считать одним. |
| `--chunk-candidates` | `4096` | Размер батча при векторных вычислениях (производительность, на результат не влияет). |

</details>

<details>
<summary><b>Штрафы ранжирования</b> — как алгоритм выбирает лучшего кандидата (тонкая настройка)</summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--corr-weight` | `1.0` | Вес корреляции в общем счёте кандидата. |
| `--offset-penalty` | `0.03` | Штраф за большой сдвиг кандидата от центра поиска. |
| `--theta-penalty` | `0.03` | Штраф за большой поворот в режиме `history`. |
| `--rank-penalty` | `0.04` | Штраф за низкий ранг (приоритет более совпавшим). |
| `--innovation-penalty` | `0.20` | Штраф за расхождение с предсказанием Калмана. |
| `--segment-innovation-penalty` | `0.25` | То же в режиме `segment`. |
| `--flat-penalty` | `0.01` | Штраф гипотезе, попавшей на ровный участок. |
| `--no-candidate-penalty` | `0.1` | Штраф гипотезе, для которой не нашлось кандидата. |

</details>

<details>
<summary><b>Фильтр Калмана</b> — «следящий глаз», ведущий цель</summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--init-pos-sigma` | `3.0` | Начальная неуверенность по позиции (пиксели). |
| `--init-vel-sigma` | `3.0` | Начальная неуверенность по скорости. |
| `--accel-sigma` | `0.05` | Допускаемая «резкость» манёвров. Больше — манёвреннее, но шумнее. |
| `--meas-sigma-min` | `1.0` | Доверие к самым качественным измерениям (мин. шум, пиксели). |
| `--meas-sigma-max` | `25.0` | Доверие к слабым измерениям (макс. шум). |

</details>

<details>
<summary><b>Постобработка</b> — улучшение готового трека (без вмешательства в основной алгоритм)</summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--smooth` / `--no-smooth` | вкл. | RTS-сглаживание трека с учётом уверенности (гасит кратковременные «срывы»). |
| `--final-fix` / `--no-final-fix` | вкл. | Защёлкнуть финальную точку на лучшее совпадение рельефа. |
| `--final-fix-window` | = `--window-size` | Длина участка для финальной доводки. |
| `--final-fix-radius-px` | `3.0` | Радиус поиска пика вокруг финальной оценки. |
| `--final-fix-res-px` | `0.1` | Разрешение этого поиска (субпиксельное). |
| `--final-fix-min-roughness` | `0.5` | Минимальная «изрезанность» рельефа, чтобы доверять доводке. |
| `--refit` | выкл. | Доводка **всего** трека по корреляции (точнее, но +~1 мин). |
| `--refit-step` | `8` | Ставить опорную точку каждые N отсчётов. |
| `--refit-radius-px` | `2.0` | Ограничение поиска вокруг текущей оценки (защита от «дорисовки»). |
| `--refit-res-px` | `0.2` | Разрешение поиска при доводке. |
| `--refit-gate-px` | `1.5` | Отвергать поправку больше этой (защита от ложного пика на неоднозначном рельефе). |
| `--refit-base-sigma` | `4.0` | Доверие к текущему пути между опорами. |
| `--refit-anchor-sigma` | `0.5` | Доверие к принятым опорным точкам. |

</details>

<details>
<summary><b>Графики и диагностика</b></summary>

| Флаг | По умолчанию | Что делает |
|---|---|---|
| `--plot-heatmap` / `--no-plot-heatmap` | вкл. | Рисовать тепловую карту захвата (направление × скорость). |
| `--heatmap-az-step-deg` | `1.0` | Шаг направления на тепловой карте. |
| `--heatmap-speed-step-px-s` | `0.2` | Шаг скорости на тепловой карте. |
| `--profile` | выкл. | Записать тайминги этапов в `result/profile_summary.json`. |

</details>

---

## Вспомогательные части (наброски и эксперименты)

Эти папки — подготовка данных и исследовательские прототипы. Они помогали прийти к финальному решению,
но это **черновики**, а не готовый продукт:

- **`траектория/`** — программа с окошком (GUI): открываешь карту высот, кликами рисуешь маршрут, она
  считает длину/азимут и сохраняет «эталонную» траекторию. Нужна, чтобы готовить тестовые данные.
  Внутри есть `архив (мусор)/` — отброшенные ранние версии.
- **`Егор/`** — генератор реалистичного шумного сигнала радиовысотомера из эталонной траектории
  (имитирует вибрацию, турбулентность, выбросы) и эксперименты с фильтрами. Подпапка
  `для тигра клодовича/final/` — **копия** финального решения из `егор чекни этот алгоритм/`.
- **`Александр/`** — отдельные реализации фильтров высоты (медиана + Калман) и генераторов помех.
- **`парсинг данных и вычисление высоты рельефа/`** — учебная/референсная реализация разбора
  NMEA-сообщений радиовысотомера и расчёта высоты рельефа (на Python и C++).

> Примечание: в финальном конвейере вся фильтрация уже встроена в `tercom_final.py`, поэтому отдельные
> фильтры из этих папок дублируют его и нужны больше для понимания принципа.

Техническое задание — в PDF в корне репозитория (`Описание первичное.pdf`, `Расширенное описание.pdf`).

---

## Краткий словарь терминов

- **TERCOM** — навигация сопоставлением профиля рельефа с картой высот.
- **DEM** — цифровая карта высот (матрица «высота в каждой точке местности»).
- **Радиовысотомер** — прибор, меряющий расстояние от самолёта до земли под ним.
- **NMEA / GPGGA** — текстовый формат, в котором приборы выдают свои данные строками.
- **Корреляция** — числовая мера схожести формы двух кривых (здесь: профиля под нами и участка карты).
- **Фильтр Калмана** — алгоритм, который сглаживает шумные измерения, предсказывая следующее состояние.
